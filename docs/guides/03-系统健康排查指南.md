# 系统健康排查与优化指南

> 项目混乱时的诊断与恢复流程 - 定期健康检查与问题修复

**文档性质**: 独立参考指南（可复用于任何项目）
**版本**: v2.0
**适用场景**: 文档混乱、代码冗余、误操作恢复、定期健康检查

---

## 目录

1. [排查概述](#1-排查概述)
2. [混乱信号识别](#2-混乱信号识别)
3. [排查检查清单](#3-排查检查清单)
4. [优化策略](#4-优化策略)
5. [恢复机制](#5-恢复机制)
6. [预防措施](#6-预防措施)
7. [实际案例](#7-实际案例)

---

## 1. 排查概述

### 1.1 什么是系统排查

对项目进行全面健康检查：
- **文档系统**：完整性、一致性、冗余
- **代码质量**：Lint 检查、未使用依赖
- **依赖健康**：过时依赖、安全漏洞
- **Git 历史**：commit 质量、未提交文件

### 1.2 排查时机

| 类型 | 触发条件 | 执行命令 |
|------|----------|----------|
| **紧急排查** | 系统已混乱、误操作 | `/audit --full` |
| **定期排查** | 每周日、每月初 | `/audit --quick` / `/audit --full` |
| **安全排查** | 发现漏洞警告 | `/audit --security` |

### 1.3 排查优先级

```
🔴 Priority 1（立即处理）
   └─ 安全漏洞、构建失败、CONTEXT.md 不准确

🟡 Priority 2（本周处理）
   └─ 文档不同步、代码质量问题、CURRENT.md 冗余

🟢 Priority 3（月度处理）
   └─ 性能优化、历史清理、依赖更新
```

---

## 2. 混乱信号识别

### 2.1 文档系统信号

| 严重度 | 信号 | 影响 | 处理时限 |
|--------|------|------|----------|
| 🔴 Critical | CURRENT.md > 5000 tokens | AI 恢复慢，Token 浪费 | 立即 |
| 🔴 Critical | CONTEXT.md 与实际不符 | AI 理解错误 | 立即 |
| 🔴 Critical | 文档链接失效 | 信息检索困难 | 立即 |
| 🟡 Warning | CURRENT.md 冗余内容 | Token 增加 | 本周 |
| 🟡 Warning | 文档未与代码同步 | 文档价值降低 | 本周 |
| 🟡 Warning | ADR 缺失 | 决策难追溯 | 本周 |
| 🟢 Info | archive 超过 6 个月 | 磁盘占用 | 月度 |
| 🟢 Info | tech-stack.md 版本不一致 | 文档过时 | 月度 |

### 2.2 代码系统信号

| 严重度 | 信号 | 影响 | 处理时限 |
|--------|------|------|----------|
| 🔴 Critical | Lint errors > 10 | 运行时错误风险 | 立即 |
| 🔴 Critical | 安全漏洞（Critical/High） | 安全风险 | 立即 |
| 🔴 Critical | 构建失败 | 无法部署 | 立即 |
| 🟡 Warning | Lint warnings > 50 | 潜在问题 | 本周 |
| 🟡 Warning | 未使用依赖 > 5 个 | 构建产物大 | 本周 |
| 🟡 Warning | 过时依赖 > 10 个 | 错失更新 | 本周 |
| 🟢 Info | 构建时间 > 5 秒 | 开发体验差 | 月度 |
| 🟢 Info | 产物大小 > 3MB | 加载速度慢 | 月度 |

### 2.3 Git 历史信号

| 严重度 | 信号 | 影响 | 处理时限 |
|--------|------|------|----------|
| 🔴 Critical | 未提交文件 > 20 个 | 版本控制失控 | 立即 |
| 🔴 Critical | 未推送 commit > 20 个 | 数据丢失风险 | 立即 |
| 🟡 Warning | Commit message 不规范 | 历史难阅读 | 本周 |
| 🟡 Warning | 单 commit > 100 文件 | 难以回滚 | 本周 |

---

## 3. 排查检查清单

### 3.1 文档系统检查

#### AI 记忆层（`ai-context/`）

**CONTEXT.md**
- [ ] TL;DR 准确反映项目现状
- [ ] 项目阶段与实际一致
- [ ] 技术栈版本与实际一致
- [ ] 下一步任务与 CURRENT.md 一致
- [ ] 快速导航链接有效
- [ ] Token < 3000

**CURRENT.md**
- [ ] 本周时间范围准确
- [ ] Day-by-day 日志完整
- [ ] 无冗余内容
- [ ] 已完成任务已标记 ✅
- [ ] Token < 2500
- [ ] 是否需要归档（超过 1 个月）

**archive/**
- [ ] 归档命名规范（YYYY-MM.md）
- [ ] 检查超过 6 个月的归档

#### 开发文档层（`development/`）

**前端**
- [ ] components.md 包含所有组件
- [ ] pages.md 包含所有页面
- [ ] troubleshooting.md 无过时问题

**后端**
- [ ] api.md 包含所有 API 端点
- [ ] database.md 与实际 Schema 一致
- [ ] troubleshooting.md 无过时问题

#### 架构层（`architecture/`）

- [ ] tech-stack.md 版本与依赖文件一致
- [ ] 重大决策有 ADR 记录
- [ ] ADR 索引完整

### 3.2 代码系统检查

```bash
# ===== 代码质量 =====
# Lint 检查（根据实际技术栈调整）
cd apps/frontend && ${PM_RUN} lint

# 未使用依赖
cd apps/frontend && ${PM_EXEC} depcheck

# TODO/FIXME 统计
grep -r "TODO\|FIXME" apps/ --include="*.js" --include="*.jsx" --include="*.ts" --include="*.tsx" --include="*.java" --include="*.py" | wc -l

# ===== 依赖健康 =====
# 过时依赖
cd apps/frontend && ${PM} outdated

# 安全漏洞
cd apps/frontend && ${PM} audit

# ===== 性能指标 =====
# 构建时间
cd apps/frontend && time ${PM_RUN} build

# 产物大小
du -sh apps/frontend/dist
```

### 3.3 Git 历史检查

```bash
# 未提交文件
git status --short | wc -l

# 最近 20 个 commit
git log -20 --oneline

# 本周 commits
WEEK_START=$(date -v-$(( $(date +%u) - 1 ))d +%Y-%m-%d)
git log --since="$WEEK_START" --oneline | wc -l

# Commit 粒度检查
git log -10 --stat --oneline
```

---

## 4. 优化策略

### 4.1 文档优化

#### 删除冗余内容

```markdown
# ❌ 冗余（删除）
### Day 1
- ✅ 修改 Header.jsx 第 45 行，padding 20px → 24px
- ✅ 修改 Header.css 第 12 行，margin 10px → 12px
- ✅ 测试 Header 组件
- ✅ 提交代码
- ✅ 推送到远程

# ✅ 优化后
### Day 1
- ✅ 优化 Header 组件样式（padding 24px, margin 12px）
```

**原则**：
- 删除过度详细的日志
- 合并重复条目
- 保留关键信息（做了什么、为什么、结果）

#### 更新过时内容

检查并更新：
- CONTEXT.md 的项目阶段
- tech-stack.md 的版本
- ROADMAP.md 的进度

### 4.2 代码优化

#### 修复 Lint 错误

```bash
# 查看所有 errors
cd apps/frontend && ${PM_RUN} lint

# 自动修复
cd apps/frontend && ${PM_RUN} lint --fix
```

#### 删除未使用依赖

```bash
# 检查（需人工判断，depcheck 可能误报）
cd apps/frontend && ${PM_EXEC} depcheck

# 删除
${PM} remove <package-name>
```

#### 更新依赖

```bash
# Patch 版本（安全更新，建议立即）
${PM} update

# Minor 版本（推荐更新）
${PM} update --latest <package-name>

# Major 版本（谨慎，需测试）
${PM} add <package-name>@latest
```

### 4.3 Git 优化

#### 提交未提交文件

```bash
git add .
git commit -m "chore: 清理未提交文件"
git push
```

#### 规范 Commit Message

**从现在开始**使用 Conventional Commits：
```
<type>(<scope>): <subject>

type: feat | fix | docs | refactor | perf | test | chore
```

**注意**：不要 rebase 已推送的 commit。

---

## 5. 恢复机制

### 5.1 误删除文件恢复

```bash
# 1. 查找文件历史
git log --all --full-history -- <file-path>

# 2. 恢复文件（假设 commit 是 abc1234）
git checkout abc1234 -- <file-path>

# 3. 提交恢复
git add <file-path>
git commit -m "fix(docs): 恢复误删除的 <file-name>"
```

### 5.2 文档内容损坏恢复

```bash
# 1. 查看文件修改历史
git log -p <file-path>

# 2. 对比历史版本（假设正常版本是 def5678）
git show def5678:<file-path> > /tmp/file_backup
diff <file-path> /tmp/file_backup

# 3. 恢复到历史版本
git checkout def5678 -- <file-path>

# 4. 提交恢复
git add <file-path>
git commit -m "fix(docs): 回滚 <file-name> 到正常状态"
```

### 5.3 放弃所有未提交修改

```bash
# ⚠️ 警告：会永久丢失未提交的修改
git reset --hard HEAD
git clean -fd
```

### 5.4 系统性恢复流程

当系统严重混乱时：

```
Step 1: 评估混乱程度
        └─ /audit --full

Step 2: 创建备份分支
        └─ git checkout -b backup-before-recovery
        └─ git push origin backup-before-recovery

Step 3: 按优先级修复
        └─ 🔴 Critical 问题先修复
        └─ 🟡 Warning 问题本周修复

Step 4: 使用 Slash Commands 恢复
        └─ /weekly（优化文档）
        └─ /monthly（如果需要归档）

Step 5: 验证恢复
        └─ /audit --full
        └─ 确认健康度评分提升
```

---

## 6. 预防措施

### 6.1 定期检查机制

| 时间 | 操作 | 命令 |
|------|------|------|
| 每周日 | 快速健康检查 + 文档优化 | `/audit --quick` → `/weekly --push` |
| 每月 1 日 | 完整健康检查 + 归档 | `/audit --full` → `/monthly --push` |

### 6.2 文档更新规范

| 事件 | 必须更新的文档 |
|------|----------------|
| 新增组件 | `components.md` |
| 新增页面 | `pages.md` |
| 新增 API | `api.md` |
| 修复 Bug | `troubleshooting.md` |
| 重大决策 | `adr/XXX.md` |
| 会话结束 | `/end` 自动更新 |

### 6.3 Git Hooks（可选）

**pre-commit hook** 示例：

```bash
#!/bin/bash
# .git/hooks/pre-commit

# Lint 检查
echo "Running lint..."
cd apps/frontend && ${PM_RUN} lint
if [ $? -ne 0 ]; then
    echo "❌ Lint failed. Fix errors before committing."
    exit 1
fi

echo "✅ Pre-commit checks passed."
```

### 6.4 代码质量门禁

| 规则 | 阻断级别 |
|------|----------|
| Lint errors > 0 | ❌ 禁止 commit |
| 安全漏洞 Critical/High | ❌ 禁止部署 |
| 构建失败 | ❌ 禁止 commit |
| Lint warnings > 50 | ⚠️ 警告 |

---

## 7. 实际案例

### 案例 1: CURRENT.md 膨胀

**背景**：
- 项目运行 2 个月，CURRENT.md 未归档
- Token 从 ~2000 增长到 ~8000
- AI 恢复上下文时间从 30 秒增加到 2 分钟

**信号识别**：
- `/start` 执行时间明显变长
- `/audit` 报告：CURRENT.md Token > 8000

**解决方案**：
```bash
# 1. 执行归档
/monthly

# 2. 手动优化归档文件（删除过度详细日志）

# 3. 更新 CONTEXT.md 时间戳
```

**结果**：
- CURRENT.md Token：~8000 → ~1500（减少 81%）
- AI 恢复时间：2 分钟 → 30 秒

**预防**：每月执行 `/monthly`

---

### 案例 2: 文档与代码不同步

**背景**：
- 新增了 5 个组件，但未更新 components.md
- AI 推荐使用不存在的旧组件

**信号识别**：
- AI 建议使用已删除的组件
- `/audit` 报告：components.md 缺失 5 个组件

**解决方案**：
```bash
# 1. 对比实际组件和文档
find apps/frontend/src/components -name "*.jsx" | wc -l

# 2. 补充缺失的组件文档

# 3. 提交更新
/checkpoint --commit
```

**结果**：
- components.md 与代码 100% 同步
- AI 推荐正确

**预防**：新增组件时立即更新文档

---

### 案例 3: Git 历史混乱

**背景**：
- 最近 20 个 commit 都是 "fix"、"update"
- 无法追溯功能添加时间

**信号识别**：
```bash
git log -20 --oneline
# 输出全是低质量 message
```

**解决方案**：
1. **不修改已推送的 commit**
2. 从现在开始使用 Conventional Commits
3. 使用 `/checkpoint --commit` 和 `/end --push` 自动生成规范 commit

**结果**：
- 新 commit 全部规范化
- Git 历史可读性提升

**预防**：使用 Slash Commands 生成 commit

---

## 附录

### 快速排查命令

```bash
# ===== 文档检查 =====
wc -l docs/ai-context/CURRENT.md                    # Token 估算
grep -r "\[.*\](.*\.md)" docs/ | grep -v "http"     # 链接检查

# ===== 代码检查 =====
cd apps/frontend && ${PM_RUN} lint                  # Lint
cd apps/frontend && ${PM_EXEC} depcheck             # 未使用依赖
cd apps/frontend && ${PM} outdated                  # 过时依赖
cd apps/frontend && ${PM} audit                     # 安全漏洞

# ===== Git 检查 =====
git status --short                                  # 未提交文件
git log -20 --oneline                               # 最近 commit

# ===== 性能检查 =====
cd apps/frontend && time ${PM_RUN} build            # 构建时间
du -sh apps/frontend/dist                           # 产物大小
```

### 恢复命令

```bash
# 恢复误删文件
git checkout <commit-hash> -- <file-path>

# 放弃未提交修改（⚠️ 谨慎）
git reset --hard HEAD
git clean -fd

# 创建备份分支
git checkout -b backup-before-recovery
git push origin backup-before-recovery
```

---

**版本**: v2.0
**更新日期**: 2025-11-26
