# 首页设计

> 仪表盘风格概览页，快速了解项目状态

**最后更新**: 2025-12-10

---

## 一、页面目标

| 目标 | 说明 |
|-----|------|
| **数据概览** | 统计卡片展示关键指标 |
| **快速导航** | 快速入口直达常用功能 |
| **状态感知** | 最近活动和项目进度一目了然 |

---

## 二、页面结构

```
首页 (/)
├── 统计卡片区（4 个）
│   ├── 族库总数
│   ├── 清单匹配率
│   ├── 本月计算量
│   └── 待处理问题
│
├── 双栏布局
│   ├── 左：快速入口（2x2 网格）
│   └── 右：最近活动列表
│
├── 核心流程区
│   └── 族 → 品目 → 清单 → 工程量
│
└── 项目阶段区
    └── 原型评审 → 开发准备 → 正式开发
```

---

## 三、统计卡片区

### 3.1 布局

4 列网格（`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`）

### 3.2 卡片设计

```
┌─────────────────────────────────┐
│  ┌────┐                         │
│  │ 📦 │  1,247        [+12%]   │
│  └────┘  族库总数               │
│  (图标)  (数值)       (趋势)    │
└─────────────────────────────────┘
```

### 3.3 数据结构

```javascript
const stats = [
  { label: '族库总数', value: '1,247', change: '+12%', trend: 'up', icon: '📦' },
  { label: '清单匹配率', value: '94.2%', change: '+3.1%', trend: 'up', icon: '🎯' },
  { label: '本月计算量', value: '28,450', change: '+18%', trend: 'up', icon: '📊' },
  { label: '待处理问题', value: '3', change: '-2', trend: 'down', icon: '🔔' },
]
```

### 3.4 样式

- 卡片: `.card` 类
- 图标: 12x12 圆角背景 `bg-bg-active`
- 数值: `text-2xl font-bold font-mono`
- 趋势: `.badge-green` (up) / `.badge-red` (down)

---

## 四、快速入口区

### 4.1 布局

2x2 网格（`grid-cols-2 gap-3`）

### 4.2 卡片设计

```
┌─────────────────────────────────┐
│  🔗  清单匹配规则      →       │
│      5 种匹配方式              │
└─────────────────────────────────┘
```

### 4.3 数据结构

```javascript
const quickLinks = [
  { label: '清单匹配规则', desc: '5 种匹配方式', icon: '🔗', path: '/logic' },
  { label: '工程量计算', desc: '4 种计算类型', icon: '🧮', path: '/logic' },
  { label: '业主清单梳理', desc: '麦当劳 / 奥乐齐', icon: '📋', path: '/quantity/owner' },
  { label: '水管计算规则', desc: '12月核心任务', icon: '🔧', path: '/quantity/pipe' },
]
```

### 4.4 样式

- 背景: `bg-bg-hover/50`
- 悬浮: `hover:bg-bg-hover hover:border-border`
- 箭头: 悬浮时右移 `group-hover:translate-x-1`

---

## 五、最近活动区

### 5.1 布局

垂直列表（`space-y-1`）

### 5.2 列表项设计

```
┌─────────────────────────────────────────┐
│  ●  上传新族文件             10 分钟前  │
│     风机盘管_标准型.rfa                 │
└─────────────────────────────────────────┘
```

### 5.3 数据结构

```javascript
const recentActivities = [
  { type: 'upload', title: '上传新族文件', desc: '风机盘管_标准型.rfa', time: '10 分钟前', status: 'success' },
  { type: 'match', title: '清单匹配完成', desc: '麦当劳-浦东店 / 127 条清单', time: '1 小时前', status: 'success' },
  { type: 'calc', title: '工程量计算', desc: '水管系统 / 3,450 m', time: '2 小时前', status: 'success' },
  { type: 'issue', title: '新问题待讨论', desc: '变径管件计算规则确认', time: '3 小时前', status: 'warning' },
]
```

### 5.4 样式

- 状态点: `.status-dot` + `.success` / `.warning`
- 时间: `text-xs text-text-tertiary`
- 悬浮: `hover:bg-bg-hover/50`

---

## 六、核心流程区

### 6.1 布局

水平流程图，居中对齐

### 6.2 设计

```
┌────────┐     ┌────────┐     ┌────────┐     ┌────────┐
│   族   │ ──▶ │  品目  │ ──▶ │  清单  │ ──▶ │ 工程量 │
│ Family │     │  Item  │     │  Bill  │     │Quantity│
│  蓝色  │     │  紫色  │     │  绿色  │     │  橙色  │
└────────┘     └────────┘     └────────┘     └────────┘
```

### 6.3 数据结构

```javascript
const coreFlow = [
  { label: '族', sub: 'Family', color: 'bg-accent-blue' },
  { label: '品目', sub: 'Item', color: 'bg-accent-purple' },
  { label: '清单', sub: 'Bill', color: 'bg-accent-green' },
  { label: '工程量', sub: 'Quantity', color: 'bg-accent-amber' },
]
```

### 6.4 样式

- 节点: `w-16 h-16 sm:w-20 sm:h-20 rounded-2xl`
- 文字: `text-lg sm:text-xl font-bold text-white`
- 悬浮: `hover:scale-105 hover:shadow-lg`
- 箭头: SVG `text-text-tertiary`

---

## 七、项目阶段区

### 7.1 布局

水平时间线，带进度条

### 7.2 设计

```
    ●────────────●────────────○
 原型评审     开发准备     正式开发
 (完成)       (进行中)      (待定)
```

### 7.3 数据结构

```javascript
const phases = [
  { label: '原型评审', date: '2025.11', done: true },
  { label: '开发准备', date: '2025.12', current: true },
  { label: '正式开发', date: '待定', done: false },
]
```

### 7.4 样式

- 完成: `bg-accent-green border-accent-green` + 对勾图标
- 进行中: `border-accent-blue` + 脉冲圆点 `animate-pulse`
- 待开始: `border-border` + 空心
- 进度条: `bg-accent-green` 动态宽度

---

## 八、响应式设计

| 断点 | 统计卡片 | 快速入口 | 最近活动 |
|-----|---------|---------|---------|
| 手机 | 1列 | 2列 | 全宽 |
| 平板 | 2列 | 2列 | 双栏右侧 |
| 桌面 | 4列 | 2列 | 双栏右侧 |

---

## 变更记录

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| v1.0 | 2025-11-25 | 初稿 |
| v2.0 | 2025-11-27 | 简化结构，Google风格重设计 |
| v3.0 | 2025-12-10 | 仪表盘风格，统计卡片+快速入口+活动列表 |
